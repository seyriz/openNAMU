extends wiki
block content
  h1.title 모든 문서 보기
  ul
    - for(var property in doc)
      li
        a(href="/w/#{property}")= property
block menu
  nav.menu
    a.menu-item(onclick='history.back(-1)', aria-label='전에 보던 곳으로 이동합니다.') 이전으로
